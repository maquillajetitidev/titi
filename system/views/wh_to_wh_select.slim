== slim :orders_list, locals: {edit_link: "/admin/transport/departures/wh_to_wh/order.o_id/add", show_filters: false, orders: orders}

form action=url("/transport/departures/wh_to_wh/new") method="post"
  == Rack::Csrf.tag(env)
  input type="submit" value="#{t.order.new}"
